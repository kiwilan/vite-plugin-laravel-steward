{
  "name": "markdoc-content",
  "scripts": {
    "dev": "cd test && pnpm run dev",
    "build": "cd lib && pnpm run build",
    "package-locally": "cd lib && pnpm pack",
    "package": "./bin/package.sh",
    "prepare": "husky install",
    "lint": "pnpm recursive run lint",
    "lint:fix": "pnpm recursive run lint:fix",
    "test": "pnpm recursive run test",
    "coverage": "pnpm recursive run coverage"
  },
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.2"
  }
}